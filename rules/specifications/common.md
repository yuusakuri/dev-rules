# 仕様書共通規則

> 適用対象: 領域を問わず、仕様書（API、UI、通信プロトコル、ハードウェアインターフェース、要求、安全要求など）を新規作成または変更する場合。

## 1. 適用範囲

仕様書は、ソフトウェアのAPIやUIに限らない。要求仕様書、システム仕様書、ハードウェアインターフェース仕様書、通信プロトコル仕様書、安全仕様書、機構仕様書など、対象領域の数だけ存在する。本書は、それら領域を問わず仕様書全般に共通する規則を定義する。

API仕様書とUI仕様書には、本書に加えてそれぞれの領域固有規則（[API仕様書作成ルール](api.md)、[UI仕様書作成規約](ui.md)）を適用する。それ以外の領域で専用の規則がまだ存在しない場合は、本書の規則だけを適用する。

仕様書は、実装が従うべき正式な契約である。実装を仕様書へ合わせ、実装を起点に仕様書を書き換えない。

---

## 2. 記述

### 曖昧な表現を避ける

「適宜」「必要に応じて」「基本的に」のような、読み手の解釈に依存する表現を使わない。判定条件、数値、具体的な挙動として記述する。

NG: 一定時間応答がなければ警告する OK: 5秒以内に応答がない場合、警告灯を点灯する

### 観測可能な挙動を記述し、実現手段を記述しない

外部から観測できる挙動（入力に対する出力、状態遷移、タイミング、許容誤差）を記述し、実現手段（使用するアルゴリズム、部品、内部設計）は記述しない。実現手段を仕様書へ書くと、実装の変更のたびに仕様書の更新が必要になり、乖離しやすくなる。

### 一つの記述には一つの意味だけを持たせる

1つの条項が複数の解釈を許すと、実装者ごとに異なる実装になる。

### 正常系だけでなく境界と例外を記述する

正常系に加え、上限、下限、タイムアウト、異常入力、通信断、権限がない場合など、境界と異常系を記述する。省略は実装漏れとテスト漏れに直結する。

---

## 3. 識別子

仕様書が扱う対象（画面、部品、メッセージ、状態、信号、コマンド、要求など）には一意な識別子を付け、実装、テスト、障害報告から仕様書の該当箇所を追跡できるようにする。

識別子は `<PREFIX>-<DOMAIN>-<NNN>` の形式にする。PREFIXで対象の種別、DOMAINで機能領域を表す。一度公開した識別子は、対象が削除された後も再利用しない（欠番として扱う）。

| 対象 | 識別子例 |
| --- | --- |
| 画面 | `SCR-USER-001` |
| 通信フレーム | `FRM-TELEMETRY-003` |
| ハードウェア信号 | `SIG-BRAKE-002` |
| 安全要求 | `SR-COLLISION-001` |

---

## 4. ファイル構成

1つの対象領域（リソース、画面、インターフェース、サブシステムなど）につき1ファイルとする。担当領域が分かれる大規模開発では、コンフリクトとレビュー範囲を最小化するため。

ファイル名は、対象を指す既存の識別子や名称をそのまま使う。仕様書の外からでも、どのファイルを見ればよいかが一意に判断できるようにする。

対象数が少ない開発初期は単一ファイルへの記載を許容し、対象数が増えて分割が必要になった時点でファイルを分割する。

---

## 5. メタデータ

すべての仕様書ファイルは、少なくとも次のメタデータを持つ。

| 項目 | 内容 |
| --- | --- |
| バージョン | セマンティックバージョニング（`MAJOR.MINOR.PATCH`）。 |
| ステータス | `draft` / `review` / `stable` / `deprecated` のいずれか。 |
| 最終更新日 | ISO 8601形式（`YYYY-MM-DD`）。 |
| 責任者 | 仕様の最終決定権を持つ人物。 |

責任者とステータスが不明な仕様書は、読み手が「今どの段階の、誰の決定か」を判断できず、古い仕様を参照し続ける原因になる。

---

## 6. 重複の禁止

複数の対象に共通する規則は共通規則として1箇所へ記載し、個別の仕様書には差分だけを記載する。同じ規則を対象ごとに重複して書くと、変更のたびにすべての箇所を修正する必要が生じる。

---
